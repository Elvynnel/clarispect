import { Aboreto, Berkshire_Swash } from 'next/font/google';

export const berkshire = Berkshire_Swash({
	subsets: ['latin'],
	variable: '--font-berkshire',
	weight: '400',
});

export const aboreto = Aboreto({
	subsets: ['latin'],
	variable: '--font-aboreto',
	weight: '400',
});
